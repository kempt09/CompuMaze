var Game = Game || {};

Game.storyGenerator = function(scene, next){

    'use strict';

    if (scene === 'init'){

        //get first scene

    } else if (next){

        //get next scene

    }

};